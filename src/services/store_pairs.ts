import ccxt, { Exchange } from "ccxt";
// import { createExchange } from "./initExchange.ts";

const kv = await Deno.openKv();

async function savePairs(exchangeId: string, pairs: string[]) {
  // Implémente la logique pour enregistrer les paires dans ta base de données
}
